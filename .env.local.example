# Copy this file to .env.local (git-ignored, never bundled into a packaged extension) and
# fill in your own key from https://console.groq.com/keys. A key entered in the panel's
# Settings tab always takes priority over this file.
GROQ_API_KEY=gsk_your_key_here
